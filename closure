//模試で振るわなかったので頻度落ちてた…（言い訳）
function closure(init) {
    let counter = init;
    
    return function() {
        return ++counter;
    }
}

let myClosure = closure(1)
console.log(myClosure());
2
console.log(myClosure());
3
console.log(myClosure());
4
